<%= form_for(@alsobought) do |f| %>
  <% if @alsobought.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alsobought.errors.count, "error") %> prohibited this alsobought from being saved:</h2>

      <ul>
      <% @alsobought.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
