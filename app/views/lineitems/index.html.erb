<p id="notice"><%= notice %></p>

<h1>Cart Contents</h1>

<table>
  <thead>
    <tr>
      <th>Product</th>
      <th>Quantity</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lineitems.each do |lineitem| %>
      <tr>
        <td><%= lineitem.product.name %></td>
        <td><%= lineitem.quantity %></td>
        <td><%= lineitem.product.price * lineitem.quantity %></td>
        <td><%= link_to 'Edit', edit_lineitem_path(lineitem) %></td>
        <td><%= link_to 'Remove', lineitem, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<tr>
  <td></td>
  <td></td>
  <td></td>
</tr>

Cart Total Items  :<%= @current_buyer.cart_total_items %>
Cart Total Amount :<%= @current_buyer.cart_total_amount %>


<br>

<td><%= button_to 'Pay Now', new_charge_path, method: :get %></td>









